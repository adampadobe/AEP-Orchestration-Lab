# Moved

This directory was renamed to [`../loyalty-reward-provider/`](../loyalty-reward-provider/).

Use:

```bash
npm run loyalty-provider:deploy
npm run loyalty-provider:test
```

Legacy aliases `npm run fake-loyalty:*` still work.
