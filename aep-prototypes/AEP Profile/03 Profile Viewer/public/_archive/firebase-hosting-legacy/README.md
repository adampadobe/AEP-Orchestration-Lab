# Archived: Firebase images (local Hosting manager)

This bundle was the **“Firebase images (in development)”** tool: browse local disk folders, preview images, upload, rename, and trigger `firebase deploy` for the Hosting `web/` tree.

It is **retired from the main Tools menu** in favor of **[Image hosting](../image-hosting.html)** (CDN library, brand scraper, and related flows).

- **Live HTML:** [`firebase-hosting.html`](./firebase-hosting.html) (same folder as `hosting.css` / `hosting-client.js`)
- **Note:** `/api/hosting/*` routes only work when the Profile Viewer dev server (or equivalent) is running; see in-app messaging on the archived page.
