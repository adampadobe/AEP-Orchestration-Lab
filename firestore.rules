rules_version = '2';
// Consent connection documents are read/written only via Cloud Functions (Admin SDK).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
